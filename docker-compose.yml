services:
  client:
    build:
      context: ./client
    container_name: skilltrack_client
    restart: unless-stopped
    networks:
      - skilltrack
      - nginx_proxy

  server:
    build:
      context: ./server
    container_name: skilltrack_server
    restart: unless-stopped
    env_file:
      - ./server/.env
    depends_on:
      - db
    networks:
      - skilltrack
      - nginx_proxy

  db:
    image: postgres:15
    container_name: skilltrack_db
    env_file:
      - ./server/.env
    restart: unless-stopped
    volumes:
      - skilltrack_pgdata:/var/lib/postgresql/data
    networks:
      - skilltrack

networks:
  skilltrack:
    name: skilltrack

  nginx_proxy:
    external: true

volumes:
  skilltrack_pgdata:
    name: skilltrack_pgdata